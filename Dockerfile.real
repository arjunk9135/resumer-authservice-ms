# Dockerfile

FROM node:18-alpine

# Set working directory
WORKDIR /app

# Copy only package.json and package-lock.json to install dependencies
COPY package*.json ./

# Install dependencies inside Docker (to ensure compatibility)
RUN npm install

# Now copy the rest of the app
COPY . .

# Expose port and start
EXPOSE 3000
CMD ["npm", "start"]
